services:
  - type: web
    name: portfolio-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python backend/ingest.py
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11
      - key: OPENAI_API_KEY
        sync: false
      - key: OPENAI_BASE_URL
        value: https://openrouter.ai/api/v1
      - key: OPENAI_CHAT_MODEL
        value: openai/gpt-4o-mini
      - key: EMBEDDING_MODEL
        value: all-MiniLM-L6-v2
      - key: CHROMA_PATH
        value: backend/data/chroma
      - key: RAG_TOP_K
        value: "4"
      - key: OPENAI_HTTP_REFERER
        sync: false
      - key: OPENAI_APP_TITLE
        value: Resume Chatbot
      - key: CORS_ALLOW_ORIGINS
        value: https://santoshroy.info,https://www.santoshroy.info,http://localhost:5173

  - type: web
    name: portfolio-frontend
    env: static
    plan: free
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_BASE_URL
        sync: false
